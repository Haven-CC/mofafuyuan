# 魔方复原项目规则

## 项目目标

- 本项目开发一个本地可运行的 3x3 魔方复原 HTML 工具。
- 用户可以按现实魔方录入 6 面颜色，网页生成对应 3D 魔方，并提供一步一步复原路径。
- 第一版优先保证：录入准确、状态校验清楚、复原步骤可跟做、3D 展示稳定。

## 技术约定

- 前端使用 `Vite + TypeScript + Three.js`。
- 3x3 求解使用成熟 JavaScript solver，目标是实用最短路径，不承诺数学绝对最优。
- 代码文件、类型、变量名使用英文；页面文案默认中文。
- 新增 UI 不做营销页，打开即进入工具。

## 目录约定

- `src/`：应用源码。
- `src/cube/`：魔方状态、颜色、求解、转法相关逻辑。
- `src/scene/`：Three.js 3D 渲染与动画。
- `src/styles.css`：全局样式。
- `dist/`、`node_modules/` 为生成物或依赖目录，不手动维护。

## 验证约定

- 改完优先运行 `npm run build`。
- 涉及 3D 或交互变化，启动本地预览并用浏览器检查。
- 不把密钥、token、密码写入项目。
